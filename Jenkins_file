pipeline {
    agent { label "jenkins_agent_VM1" }

    stages {

        stage("Test stage") {
            steps {
                echo "Pipeline works!"
                sh "ls -l"
            }
        }
    }

    post {
        success {
            archiveArtifacts("doc/source/*, dist/*.whl")
        }
    }
}
